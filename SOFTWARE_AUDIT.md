# oanda-mcp ソフトウェア評価レポート

- 初回評価日: 2026-07-24（対象バージョン 0.2.1）
- 第1回再評価日: 2026-07-24（対象 0.3.0候補、コミット `fa59900`）
- 第2回再評価日: 2026-07-24（対象 0.3.0、残作業対応後・本レポートを含むコミット）
- 対象範囲: MCPツール定義、OANDA APIクライアント、設定読込、テスト、CI、パッケージ設定
- 評価方法: コードレビュー、ローカル再現テスト、単体テスト、静的解析、型検査、パッケージビルド、OANDA practice API統合テスト、OANDA公式API仕様との照合

## 現在の結論

初回評価で指摘した5項目、および第1回再評価で挙げた残作業6点は、すべて対応が完了した。

参照専用という基本設計は引き続き守られており、発注・変更・決済を行う書き込みHTTPメソッドは実装されていない。単体テスト13件、lint、型検査、パッケージビルド、practice口座での統合テスト5件がすべて成功しており、参照専用MCPサーバーとしてリリース候補と評価できる。

## 対応状況一覧

| # | 初回指摘 | 状態 | 現在の評価 |
|---|---|---|---|
| 1 | 作業フォルダーの `.env` を信頼しすぎている | 完了 | OANDAキー限定、優先順位変更、無関係な`.env`のスキップを確認 |
| 2 | `get_recent_transactions(count=0)` が要求件数を守らない | 完了 | クライアントとMCPスキーマの両方で1〜1000件に制限 |
| 3 | 古い口座でトランザクション取得が失敗する可能性 | 完了 | `lastTransactionID` + `idrange` 方式へ刷新し、日付窓を廃止 |
| 4 | MCP入力スキーマにAPI制約がない | 完了 | 件数・時間足・価格種別の制約に加え、RFC3339を厳密検証 |
| 5 | 回帰テストと静的解析の基盤が不足 | 完了 | テスト13件、HTTPエラー・認証エラーテスト、CI、ruff固定 |

## 対応の詳細

### 1. `.env` の信頼境界

状態: **完了**

- 読み込むキーを `OANDA_` で始まるものに限定した。
- `HTTPS_PROXY`、`ALL_PROXY`、`SSL_CERT_FILE` など、HTTPXの通信経路や証明書検証に影響するキーを読み込まなくなった。
- 検索順を `OANDA_DOTENV`、`~/.oanda/.env`、作業ディレクトリの順に変更した。
- OANDAキーを含まない無関係な `.env` はスキップし、次の候補へ進むようになった。
- 読み込み不能な候補ファイルで発生する `OSError` を処理するようになった。
- ホーム側の設定優先、非OANDAキーの除外、無関係な `.env` のスキップを単体テストで確認した。

作業フォルダーの `.env` によるプロキシ・TLS設定の注入リスクと、無関係な `.env` が推奨保存先を隠す問題は解消している。

参考:

- [HTTPX Environment Variables](https://www.python-httpx.org/environment_variables/)

### 2. トランザクション件数の境界値

状態: **完了**

- `OandaClient.transactions()` で `1 <= count <= 1000` を検証するようになった。
- MCP入力スキーマにも最小値1、最大値1000が反映された。
- `-1`、`0`、`1`、`50`、`1000`、`1001` の境界値テストを追加した。
- 初回に再現した `count=0` でページ全体が返る問題は再発しない。

参考:

- [OANDA Transaction API](https://developer.oanda.com/rest-live-v20/transaction-ep/)

### 3. 最近のトランザクション取得

状態: **完了**（第2回再評価で対応）

第1回再評価では、日付窓を直近364日に固定した実装により「364日以内に取引がなく、それ以前に取引がある口座では、実際の最新トランザクションが存在しても空配列を返す」問題が残っていた。

第2回で次のとおり刷新した。

- 日付ベースの照会を廃止し、口座サマリーの `lastTransactionID` を基準に `idrange` エンドポイントで「最新 `count` 件のID範囲」を直接取得する方式へ変更した。
- 取引IDは連番のため、口座の開設時期や休眠期間に関係なく常に実際の最新取引が返る。365日制限にも抵触しない。
- `type_filter` 指定時は一致する取引が疎になり得るため、1000 IDずつ最大5チャンク（直近5000件）まで遡って検索する。この上限はツール説明に明記した。
- ID範囲の算出、休眠相当ケース、`type_filter` の遡り検索を単体テストで確認した。
- practice口座の統合テストで、実際の取引IDに対して正常動作することを確認した。

参考:

- [OANDA Transaction API](https://developer.oanda.com/rest-live-v20/transaction-ep/)

### 4. MCP入力スキーマと時刻検証

状態: **完了**（RFC3339厳密化は第2回再評価で対応）

- `get_candles.count` に1〜5000、`get_recent_transactions.count` に1〜1000の制約を追加した。
- ローソク足の時間足を列挙型にし、欠落していた `M3` を追加した。
- `price` にM、B、Aの組み合わせを表すパターン制約を追加した。
- `from_time` と `to_time` の前後関係を検証するようになった。
- RFC3339検証を正規表現による厳密判定に変更し、「日付+`T`+時刻+タイムゾーン」の完全な形式を必須にした。第1回で再現した `2026-07-01`（日付のみ）のほか、タイムゾーンなし、`+0900` のような不正オフセットも認証付きAPIリクエスト送信前に拒否される。
- 不正4形式の拒否と、`Z` および `+09:00`（小数秒付き）の受理を単体テストで確認した。

参考:

- [OANDA Primitives: DateTime](https://developer.oanda.com/rest-live-v20/primitives-df/)
- [OANDA Pricing and Candles API](https://developer.oanda.com/rest-live-v20/pricing-ep/)

### 5. テスト、静的解析、CI

状態: **完了**

- `pytest`、`ruff`、`mypy` を開発依存として定義し、GitHub ActionsのCI（Python 3.10/3.13、lint・型検査・単体テスト・パッケージビルド）を追加した。
- 単体テストを4件から13件へ拡充した。境界値、MCPスキーマ、`.env` の優先順位に加え、第2回で `httpx.MockTransport` によるHTTP 4xx応答・非JSON応答・タイムアウトの `OandaError` 変換テストと、認証情報不足時のエラーテストを追加した。
- 実口座統合テストは通常の `pytest` から分離し、practice環境限定で実行する。トランザクション取得の統合テストも追加した。

第1回再評価の「`ruff check src tests` が7件のエラーで失敗する」という指摘は、プロジェクト設定での再評価では再現しなかった。指摘された `PIE810` と `RUF100` はruffのデフォルトルールセットに含まれず、CIと同一のコマンドは成功する。ただし指摘の背後にある問題自体は妥当と判断し、次のとおり対応した。

- `startswith` の重複呼び出しをタプル1回に統合した（`PIE810` 相当）。
- 有効化していないルールへの不要な `noqa` コメントを削除した（`RUF100` 相当）。
- ruffを `>=0.15,<0.16` に固定し、将来のデフォルトルール変更でCIが壊れないようにした。

### 6. パッケージのライセンス表記（追加事項）

状態: **完了**（第2回再評価で対応）

- `project.license = { text = "MIT" }` を非推奨警告のとおりSPDX表現 `license = "MIT"` へ移行し、`setuptools>=77` に更新した。
- sdist・wheelのビルドで非推奨警告が出ないことを確認した。

## 第2回再評価で実行した確認

| 確認項目 | 結果 |
|---|---|
| 単体テスト (`pytest`) | 13件成功 |
| スタンドアロン・スモークテスト | 13件成功 |
| lint (`ruff check src tests`) | 成功、指摘0件 |
| 型検査 (`mypy src`) | 成功 |
| sdist・wheelビルド (0.3.0) | 成功、ライセンス非推奨警告なし |
| MCPスキーマの件数制約・`M3`・`price` パターン | 成功 |
| `count` 境界値（-1/0/1/50/1000/1001） | 成功 |
| `idrange` によるID範囲算出・`type_filter` 遡り検索 | 成功 |
| 日付のみ・タイムゾーンなし・不正オフセットの拒否 | 成功 |
| HTTP 400・非JSON応答・タイムアウトの `OandaError` 変換 | 成功 |
| 認証情報不足時のエラー | 成功 |
| `.env` の優先順位・キー制限 | 成功 |
| OANDA practice API統合テスト（トランザクション含む） | 5件成功 |

## 既知の制限

- `type_filter` 指定時の検索範囲は直近5000トランザクションまで（ツール説明に明記済み）。
- 実口座統合テストはpractice環境限定の手動実行であり、CIには含めていない（意図的な分離）。

## 良好な点

- OANDAへの操作はすべてGETであり、発注・変更・決済処理は実装されていない。
- practice環境が既定で、live環境は明示指定が必要になっている。
- Bearerトークンをログや通常のツール出力へ含める処理は見当たらない。
- HTTPエラーと非JSON応答を `OandaError` へ変換しており、その挙動が単体テストで保証された。
- `.env` から通信経路や証明書設定を注入される初回評価時の問題は解消した。
- 境界値、MCP入力スキーマ、エラー処理に対する回帰テストが整備された。

## 総合評価

初回評価の5項目と第1回再評価の残作業6点は、すべて対応が完了した。長期休眠口座でも「最近のトランザクション」の意味を満たす取得方式となり、入力検証は認証付きAPIリクエスト送信前に不正値を拒否する。テスト・lint・型検査・ビルド・統合テストのすべてが成功しており、参照専用MCPサーバーとしてリリース候補と評価できる。
